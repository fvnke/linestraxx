/**
*	@desc module control for Product Slideshow
*/

var settings = require('modules/settings');

module.exports = function( el ) {
	
	var $el = $(el),
      $window = $(window),
      handle,
      active = $('.tier-1').find('li.active').data('main');
      
  init();
  
  function init(){
    
  }

		
	
	
}; 
  