module.exports = function( el ) {
 	   
		var $window = $(window),
  $el = $(el);
		
    init();
    
    function init(){
      /*
      $el.on('click', function(e){
        e.preventDefault();
        e.stopPropagation();
        $('.grid-item.related').removeClass('active');
        $el.addClass('active');
        console.log('cart-related click');
        
        //$('#related-products').flickity('resize');
      });
      */
    }
    
};
  